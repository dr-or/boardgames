<nav class="navbar navbar-expand-lg navbar-dark bg-success mb-3">
  <div class="container-fluid">
    <%= link_to t("views.app_title"), games_path, class: "navbar-brand ms-sm-5 fst-italic" %>
    <div class="d-flex">
      <% if user_signed_in? %>
        <%= link_to t("views.games.index.new_game"), new_game_path(user_id: current_user),
                    class: "btn btn-warning me-3" %>
        <%= link_to current_user.name, current_user,
                    class: "btn btn-light me-3" %>
        <%= button_to t("views.layouts.sign_out"), destroy_user_session_path, method: :delete,
                      class: "btn btn-light me-md-5", data: { turbo: false } %>
      <% else %>
        <%= link_to t("views.devise.registrations.new.registration"), new_user_registration_path,
                    class: "btn btn-light me-3" %>
        <%= link_to t("views.devise.sessions.title"), new_user_session_path,
                    class: "btn btn-light me-md-5" %>
      <% end %>
    </div>
  </div>
</nav>
