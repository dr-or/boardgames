<% errors = model.errors %>

<% if model.errors.any? %>
	<ul class="alert alert-danger">
		<% model.errors.full_messages.each do |message| %>
			<li>
		  	<%= message %>
			</li>
		<% end %>
	</ul>
<% end %>
